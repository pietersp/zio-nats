target/
.bsp/
.idea/
.metals/
.bloop/
*.class
*.log

# Claude Code — ignore personal settings; project skill sources live in .skillshare/
.claude/settings.json
.claude/settings.local.json

# Generated target skill mirrors from skillshare sync -p
.agents/skills/
.claude/skills/
.opencode/skills/

# Environment variables
.env

# Coursier cache accidentally created in project root (Windows null-home fallback)
null/

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# MCP config (nix symlink, local only)
.mcp.json
