target/
.bsp/
.idea/
.metals/
.bloop/
*.class
*.log

# Claude Code — ignore personal settings; skills are project-specific and committed
.claude/settings.local.json

# Agent skills (personal, not project-specific)
.agents/skills/

# Environment variables
.env

# Coursier cache accidentally created in project root (Windows null-home fallback)
null/

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# MCP config (nix symlink, local only)
.mcp.json
